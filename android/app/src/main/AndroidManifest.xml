<?xml version="1.0" encoding="utf-8"?>
<!-- Klangten for Android (GNU GPL v3). See docs/android.md. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Self-update from the GitHub releases (Host.installPackage): the system
         package installer asks the user before anything is installed. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android 11 and newer hide other apps: without this Klangten only ever
         sees Google's engine, not Samsung/Vocalizer or any other installed one. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:label="Klangten"
        android:extractNativeLibs="false"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Serves only the verified update APK to the package installer. -->
        <provider
            android:name=".UpdateProvider"
            android:authorities="it.sixdots.klangten.updates"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
